# Devil — Feuille de route

De « ça compile » à « on y joue » : plan pour construire **Devil** sur le moteur OpenDiablo2 — architecture, priorités, et ce qu'on ne fait volontairement pas tout de suite.

> Ce document a été réactualisé après lecture de `devil_lore.md`, `devil_game_design_reference.md` et `devil_mage_character_design.md`. Devil n'est pas "Diablo 2 avec un autre skin" : classe unique, stats et formule de dégâts entièrement magiques, arbres de compétences propres, lore propre. OpenDiablo2 reste le bon choix de **moteur** (rendu, chargement d'assets, pipeline de données, réseau local client/serveur) mais son **contenu** (skills.txt, monstats.txt, items.txt tels que fournis par Blizzard) ne correspond plus à ce qu'on construit — il faudra le remplacer par du contenu propre à Devil, pas le réutiliser tel quel.

> **⚠️ Mise à jour août 2026 — pivot multi-classes.** Le studio a fourni de vrais assets visuels (turnarounds 9 angles + cycles de marche) pour trois personnages : Mage, Mage Divin (forme évoluée du Mage) et **Warrior** (combattant au corps-à-corps physique, sabres jumeaux). Devil n'est donc plus une classe unique — voir `devil_mage_character_design.md` (réécrit pour coller aux vrais assets, palette/costume différents de la version précédente) et le nouveau `devil_warrior_character_design.md`. `devil_game_design_reference.md` a été annoté en conséquence mais **pas entièrement réécrit** : ses §5/§8 (pas de Strength, pas d'armes physiques) restent vrais pour le Mage uniquement. Deux écarts code/design connus, non résolus dans cette passe :
> 1. **Contenu Go déjà écrit pour une classe unique** : `select_hero_class.go` équipe systématiquement `ItemBatonApprenti` (un bâton) alors que le Mage réel n'en tient pas dans les nouveaux assets ; les 30 compétences Devil (`d2core/d2hero/devil_skill_tree.go`) et `resolveAttackDamage` sont uniquement la formule magique du Mage — rien n'existe encore pour le Warrior (attributs, arbre, itémisation, formule de dégâts : voir `devil_warrior_character_design.md` §4).
> 2. **Assets non intégrables tels quels** : les rendus sont à hauteur d'œil (caméra type écran de sélection), pas la caméra isométrique du moteur — utilisables pour portraits/UI/menu immédiatement, pas comme sprites `.DC6` de gameplay sans nouveau rendu ou repaint (voir `docs/visual_asset_prompts.md`).

## Décision de départ

On construit sur **OpenDiablo2** (Go/ebiten) comme moteur technique, pas sur AbyssEngine (trop tôt, aucune logique de jeu) et pas depuis zéro (le pipeline DC6/DCC/COF, le chargeur MPQ, le générateur de carte et le système de tables de données `d2records` sont directement réutilisables). Le contenu — classe, compétences, monstres, objets, lore — est spécifique à Devil et se construit par-dessus.

**Cible concrète de fin de parcours** : la **Région I** (Terres dévastées) jouable de bout en bout avec la classe Mage — créer un personnage, lancer des sorts, looter et équiper des objets magiques, monter au moins l'arbre Élémentalisme, vaincre le Gardien des Cendres, entendre les dialogues de Sage Wyn, sauvegarder et recharger. Pas les 5 régions tout de suite.

## Ce qui change par rapport au plan précédent (générique D2)

| Élément | Ancien plan (D2 générique) | Plan actualisé (Devil) |
|---------|----------------------------|-------------------------|
| Classes | Plusieurs, réutiliser celles de D2 | Une seule : le Mage (H/F cosmétique) |
| Attributs | Strength/Dexterity/Vitality/Energy (D2) | Energy/Vitality/Dexterity — **pas de Strength** |
| Dégâts | Arme physique équipée (déjà câblé, Phase 1) | Formule magique liée à l'Energy — **à corriger** |
| Mitigation | Armor/Defense physique | Résistances élémentaires uniquement, cap 75%, jamais négatif |
| Compétences | "Un arbre par classe" (vague) | 30 compétences précises / 3 arbres / synergies documentées |
| Objets | Réutiliser items.txt tel quel | Objets magiques uniquement (bâtons, orbes, robes...) — items.txt à retravailler |
| Monstres/boss | Génériques D2 | Noms et lore propres (Gardien des Cendres, La Noyée...) |
| Difficultés | Normal/Nightmare/Hell | Éveil/Corruption/Apocalypse (mêmes mécaniques, autre nommage) |

## Phase 0 — Remise en état des fondations ✅
*Fait — commit local `597702f`*

Avant d'ajouter du gameplay, s'assurer que ce qui existe ne casse pas silencieusement. Ce travail est indépendant du contenu du jeu — rien à revoir ici.

- ✅ `github.com/hajimehoshi/ebiten/v2` monté de v2.0.2 (2021) à v2.9.9 ; go.mod passé à Go 1.24. Build/vet/test complets vérifiés propres avec Go 1.26.5.
- ✅ Corrigé plusieurs vrais bugs révélés par `go vet` (format strings non constantes, canal `os.Signal` non bufferisé) — bloquants pour `go test`.
- ✅ Corrigé `TestRandFunction` (`rand.Seed()` devenu no-op sous Go 1.24+).
- ✅ CI GitHub Actions : build + vet + test + `golangci-lint` (v1.64.2, `only-new-issues` pour ne pas bloquer sur les centaines de violations `wsl` préexistantes).
- ✅ Test de fumée (`d2core/d2asset/d2asset_smoke_test.go`).

## Phase 1 — Combat et attributs du Mage ✅
*Six items sur six faits et testés, y compris le round-trip complet de la sauvegarde.*

**Fait :**
- Une tranche verticale de combat de bout en bout — un monstre tuable a des PV, un cast de compétence proche de lui déclenche une résolution de dégâts côté serveur, le client reçoit et applique la mise à jour (PV ou suppression à la mort). Commits `0c9f823`, `1ebdbd3`.
- ✅ **Corrigé** : `resolveAttackDamage` (`d2networking/d2server/game_server.go`) utilisait les dégâts d'une arme physique équipée — ne correspondait pas au design (pas de Strength, pas d'arme de corps-à-corps non magique). Réécrit avec la vraie formule : `Dégâts = base_sort × (1 + Energy / 100)`, où `Energy` vient de `HeroState.Stats` et `base_sort` est un placeholder à plat (`baseSortDamage`) en attendant de vraies données de compétence (Phase 2). Commit `e5cc5d82`.

- ✅ **Trait de feu** a maintenant son propre `base_sort` (6, contre le placeholder à plat de 4) via une petite table `skillBaseSortDamage` indexée par un ID de compétence propre à Devil — délibérément numéroté loin de la plage des vrais ID de skills.txt de D2 (chargés à l'exécution depuis les MPQ du joueur, jamais présents dans ce dépôt) pour ne jamais entrer en collision. Commit `498d1fc0`.
- ✅ **Cooldown de cast lié à la Dexterity** : `canCastNow`/`castCooldownFor` — 800ms à 0 Dexterity, réduit progressivement, plancher à 200ms. Ferme un vrai trou (rien n'empêchait de spammer des `CastSkill` avant ça) et donne un premier effet réel à la Dexterity. Échelle continue en attendant les vrais paliers ("breakpoints") du design, qui demandent des données d'animation qui n'existent pas encore. Commit `d2474efc`.
- ✅ **Coût en mana + régénération** : `manaCostFor`/`manaRegenPerSecond` — chaque cast coûte du mana (3 pour Trait de feu, 2 par défaut), la régénération dépend de l'Energy (dernière stat dérivée manquante du §6, avec les dégâts et la vitesse d'incantation). `canCastNow` bloque désormais un cast si le mana est insuffisant, comme il bloque déjà sur le cooldown. Commit `6409c61f`.
- ✅ **Correction majeure (août 2026) — le mana dépensé/régénéré n'était jamais diffusé, même en solo** : trouvé en creusant le même trou que Bouclier de mana/Armure de glace/Éveil du Nexus/Absorption d'énergie (tous corrigés juste avant). Celui-ci est plus grave que les autres : `canCastNow` dépense et régénère `state.Stats.Mana` à **chaque lancer de sort**, y compris ceux qui échouent ensuite faute de mana suffisant (la régénération a lieu avant la vérification du coût) — et rien, absolument rien, ne renvoyait jamais ce nouveau total au client. Concrètement, l'orbe de mana du joueur n'aurait jamais bougé, quel que soit le nombre de sorts lancés, depuis l'introduction du système de mana en Phase 1. Corrigé par `GameServer.broadcastPlayerMana(sourceEntityID)`, appelée dans `resolveMeleeHit` juste après **chaque** appel à `canCastNow`, succès ou échec (la régénération peut avoir eu lieu même si le lancer échoue ensuite). Réutilise `PotionUsedPacket`, désormais le point de sync générique pour "le mana du joueur a changé" (potion, restauration de compétence, ou simplement un cast). Test de régression : vérifie que le paquet diffusé porte bien le mana courant.
- ✅ **Trait de feu castable en jeu** : chaque personnage créé reçoit maintenant `d2hero.NewTraitDeFeuSkill()` comme `LeftSkill` par défaut (`select_hero_class.go`). En creusant l'assignation, plusieurs points de crash latents ont été trouvés — le rendu HUD et le déclenchement de cast déréférençaient `LeftSkill`/`RightSkill` sans jamais vérifier le nil, pas seulement pour Devil, pour n'importe quelle compétence non résolue. Corrigé (`hud.go`, `game_controls.go`), avec un test qui garantit que `NewTraitDeFeuSkill()` ne peut jamais reproduire ce crash. Commit `425d3d13`.

**✅ Ancienne limite connue, corrigée (août 2026) — pire que documenté.** La note originale disait "la résolution de compétence au chargement ne connaît que les skills.txt de D2" ; en creusant, c'était en fait un **crash** : `HeroSkill.UnmarshalJSON` ne restaure que l'ID (`Shallow`), puis `LoadHeroState` et `HydrateSkills` (côté client, appelée à chaque paquet `AddPlayer`) faisaient toutes les deux `asset.Records.Skill.Details[skillID]` sans vérifier le résultat — pour un ID de compétence Devil (jamais dans les skills.txt de D2), ce lookup renvoie `nil`, et la ligne suivante déréférence ce pointeur nil (`.Skilldesc`) → panique garantie, dès le tout premier `AddPlayer`, puisque même Trait de feu de départ est un ID Devil. Ce bug n'avait simplement jamais été déclenché : cet environnement n'a pas d'affichage, donc la vraie boucle de jeu qui traite les paquets `AddPlayer` n'a jamais tourné. Corrigé dans les deux fonctions (branchement vers `NewDevilHeroSkill` pour les IDs Devil, garde nil sur le chemin D2 restant), avec deux tests de régression qui prouvent le fix en passant un `AssetManager` nil. Commit `42ded24d`.

**Reste à faire :**
- ✅ **Modificateurs d'équipement** : `d2core/d2hero/devil_items.go` — le Bâton de l'Apprenti (`+10% dégâts Feu`) a maintenant un vrai code d'objet Devil, assigné comme arme de départ à la création de personnage, et son modificateur s'applique dans `resolveAttackDamage` par-dessus la mise à l'échelle par Energy. Commit `3578d906`.
- ✅ **Correction (août 2026) — le bonus d'Energy des objets était déclaré mais jamais appliqué** : `DevilItemDef.EnergyBonus` (le "+5 Energy" du Bâton de l'Apprenti, `devil_mage_character_design.md` §5) existait depuis le premier objet Devil, mais rien ne le lisait — `resolveAttackDamage` et la régénération de mana de `canCastNow` lisaient toutes les deux `state.Stats.Energy` directement, sans jamais consulter l'objet équipé. Même catégorie de bug que le crash `HydrateSkills`/`LoadHeroState` : un modèle de données qui semblait branché mais ne l'était pas. Corrigé via un nouveau `effectiveEnergy(state)` (Energy de base + bonus de l'arme équipée en `RightHand`), utilisé de façon cohérente par les deux — limité à `RightHand` pour l'instant, faute d'emplacements amulette/anneau sur `CharacterEquipment`. Commit `06e628a4`.
- ✅ **Résistances** : `FireResist`/`ColdResist`/`LightningResist`/`ShadowResist` sur `HeroStatsState`, `CapResistance` (clampe à [0, 75], jamais négatif contrairement à D2) et `MitigateDamage` (réduction en %). `tryMonsterAttack` mitige déjà ses dégâts via `FireResist` (tous les monstres traités comme dégâts de Feu pour l'instant, faute de données élémentaires par monstre). Commit `d0c46288`.
- ✅ **Bouclier de mana** : `HeroStatsState.ManaShieldActive` + `ApplyDamageWithManaShield` — draine le mana 1:1 avant de toucher la Health, le surplus retombe sur la Health normalement. Pas encore relié à un vrai sort/toggle (viendra avec la vraie compétence Ésotérisme en Phase 2) — activé directement pour l'instant. Commit `5b5f3946`.
- ✅ **Sauvegarde** : format JSON maison pour commencer (pas la compatibilité `.d2s`, hors sujet puisque les stats/formats de personnage ne sont plus ceux de D2). `HeroStateFactory.Save`/`LoadHeroState` existent déjà et la résolution de compétence au chargement est maintenant corrigée (voir ci-dessus). **Round-trip complet vérifié (août 2026)** : maintenant qu'il y a plus d'un seul type d'objet en jeu (arme/amulette/anneau/torse, potion de ceinture, compétence à points multiples), un test dédié sauvegarde puis recharge un personnage réaliste via le vrai `Save`/`LoadHeroState` (pas un marshal fait main) et vérifie que tout survit : les 4 emplacements d'équipement, `Skills` (dont une compétence à 2 points investis via `InvestSkillPoint`), la ceinture, et les champs `HeroStatsState` les plus récents (`StatsPoints`, `VitalitySpent`, `LifePerVit`). Tout passe du premier coup — pas de bug trouvé cette fois, juste une vraie couverture là où il n'y en avait qu'une supposition documentée.

**Phase 1 close** : sur les six items listés au départ (dégâts, cooldown, mana, résistances, bouclier de mana, sauvegarde), les six sont maintenant faits et testés — les modificateurs d'équipement et la résolution de compétence au chargement (le dernier point bloquant, qui s'est avéré être un crash plutôt qu'une simple limite silencieuse) sont désormais réglés.

**Simplifications volontaires qui restent acceptables pour l'instant** (documentées en commentaire `ponytail:` dans le code) : ciblage par proximité au lieu de clic-sur-cible, pas de jet de précision.

**Correction (août 2026)** : la ligne ci-dessus affirmait encore "une seule cible par sort (pas de zone d'effet même pour les sorts qui devraient en avoir un)" — vrai à la fin de la Phase 1, plus du tout depuis la Phase 2 : `resolveAoeHit`/`killableNPCsWithin` (Boule de feu, Nova de givre, Tempête statique, Orbe glaciale, Météore, Apocalypse, Ralentissement, Prison de glace, Distorsion temporelle, Vortex, Champ statique) et `resolveChainHit` (Éclair en chaîne) couvrent tous une vraie zone ou une vraie chaîne de cibles, pas une cible unique.

## Phase 2 — Arbres de compétences du Mage 🚧
*Démarrée — 10/10 sorts d'Élémentalisme, 10/10 Arcane, 7/10 Ésotérisme (27/30 au total ; les 3 restants sont tous des invocations alliées, bloquées sur des données/sprites de monstre qui n'existent pas).*

Le design définit **30 compétences en 3 arbres de 10** (`devil_game_design_reference.md` §7) : Élémentalisme (dégâts directs), Arcane (contrôle/amplification), Ésotérisme (défense/survie/invocations), avec synergies inter-arbres et un système de palier (`niveau requis`) qui débloque les compétences supérieures à condition d'avoir investi dans les inférieures.

- ✅ **Modèle de données** : `d2core/d2hero/devil_skill_tree.go` — `DevilSkillDef` (arbre, palier, `base_sort`, coût en mana, cible/pourcentage de synergie) et le registre `DevilSkills`, plus `CanLearnSkill` pour le palier. Remplace les deux maps ad-hoc de `game_server.go` par une seule source de vérité. Commit `1f1275c0`.
- ✅ **Éclat de glace** (2e sort d'Élémentalisme) : `base_sort` plus faible que Trait de feu (4 contre 6, coût en mana plus élevé) mais un vrai effet de ralentissement — `NPC.ApplySlow`/`IsSlowed`, `ChasePlayer` réduit sa vitesse de moitié pendant l'effet. Rendu possible par l'IA de monstre de la Phase 4 (avant ça, rien n'aurait consommé un ralentissement de façon significative). Commit `43c26f4a`.
- ✅ **Éclair en chaîne** (3e sort, palier 6 — premier sort qui exploite réellement `CanLearnSkill`) : dégâts sur jusqu'à 3 cibles. A demandé de sortir `resolveMeleeHit` de son modèle mono-cible — `nearestKillableNPC` (recherche avec exclusion) et `applyHit` (dégâts/mort/or/ralentissement/diffusion) sont maintenant partagés entre le coup simple et `resolveChainHit`. **Limite de test connue** : les champs de `NPC` sont privés hors de `d2mapentity`, donc l'itération multi-cible n'est pas testée unitairement au niveau du serveur (seules les données de compétence le sont) — documenté dans le commit plutôt que masqué. Commit `9c53f951`.
- ✅ **`HeroState.LearnSkill`** : relie enfin le modèle de données à la vraie logique de jeu — dépense un point de compétence pour apprendre n'importe quel sort du registre `DevilSkills` (pas seulement Trait de feu), avec toutes les vérifications (palier via `CanLearnSkill`, points disponibles, pas déjà connu). `NewTraitDeFeuSkill` généralisé en `NewDevilHeroSkill(skillID)`. Commit `4a85e2f7`.
- ✅ **Vrai déclencheur réseau pour `LearnSkill` (août 2026)** : nouveaux paquets `LearnSkillRequestPacket`/`SkillLearnedPacket`, même forme que `UsePotionRequest`/`PotionUsed`. Referme un manque documenté depuis l'introduction de `LearnSkill` ("pas encore de vrai appelant"). **Reste manquant** : un vrai bouton d'arbre de compétences côté client qui envoie la requête. Commit `90959207`.
- ✅ **Système d'XP/niveau** (`devil_game_design_reference.md` §5 : 5 points d'attribut + 1 point de compétence par niveau) : `HeroStatsState.GrantExperience` fait monter de niveau (potentiellement plusieurs fois d'un coup) tant que `Experience` dépasse `NextLevelExp`, courbe placeholder à plat (`experienceForLevel = niveau × 100`, aucune vraie table Devil n'existe). `d2hero.RollExperienceDrop` (10-30 XP, même forme que `RollGoldDrop`) est distribué à la mort d'un monstre via `GameServer.awardExperience`, diffusé au client par le nouveau paquet `ExperienceAwardedPacket`. Donne enfin à `LearnSkill` une vraie source de points de compétence — la requête réseau existe désormais (voir plus bas), seule l'UI d'arbre de compétences manque encore. Commit `c6e1d818`.
- ✅ **Nova de givre** (4e sort, palier 6 — même palier qu'Éclair en chaîne) : première compétence centrée sur le lanceur plutôt que sur une cible/position visée — touche tous les monstres tuables dans un rayon autour du Mage. `killableNPCsWithin` généralise `nearestKillableNPC` (collecte tous les monstres en portée au lieu du seul plus proche). Même limite de test que Éclair en chaîne : le trajet multi-cible réel n'est testé qu'au niveau des données/no-op, pas de bout en bout côté serveur. Commit `8c3855ff`.
- ✅ **Boule de feu** (5e sort, palier 3/niveau 12) : AoE centrée sur la position visée plutôt que sur le lanceur — `resolveNovaHit` a été factorisé en `resolveAoeHit(centre, source, sortID, rayon)`, partagé par Nova de givre (centre = lanceur) et Boule de feu (centre = cible visée). Plus haut `base_sort` de tous les sorts d'Élémentalisme à ce stade, conforme à "dégâts feu élevés". Commit `05ad1b47`.
- ✅ **Tempête statique** (6e sort, palier 3, aux côtés de Boule de feu) : "zone d'éclair persistante" — pour l'instant résolue comme un simple coup AoE instantané sur la position visée (même chemin `resolveAoeHit` que Boule de feu), la partie "persistante" n'est pas modélisée (aucune entité zone/durée n'existe) — commentaire `ponytail` avec la voie de montée en gamme (une expiration par lancer suivie comme `lastCastAt`, re-déclenchant `resolveAoeHit` à chaque `aiTickInterval`). Le if unique de Boule de feu dans `resolveMeleeHit` est devenu `aoeAtTargetRadiusSubtiles`, une map sortID→rayon, pour ne pas empiler les if à mesure que d'autres sorts AoE-sur-cible arrivent (Orbe glaciale, Météore, Apocalypse sont du même type). Commit `c2dc45f2`.
- ✅ **Champ statique** (1er sort d'Arcane, palier 1) : "Réduit la vie de toutes les entités à l'écran d'un % fixe" — premier sort dont les dégâts ne suivent pas la formule Energy de `resolveAttackDamage` (dégâts = % de la vie actuelle de chaque monstre). "à l'écran" est modélisé comme tous les monstres tuables de la carte, faute de notion de champ de vision par client — documenté en `ponytail`. `applyHit` a été scindé en `applyHit` (dégâts Energy, inchangé) + `applyResolvedDamage` (mort/or/XP/ralentissement/diffusion, prend un montant déjà calculé) pour que Champ statique partage cette plomberie sans la dupliquer. Commit `47b74b00`.
- ✅ **Télékinésie** (2e sort d'Arcane, palier 1) : "Repousse les entités, interaction avec les objets à distance" — seule la moitié "repousse" est modélisée (la partie interaction à distance avec les objets demande le système d'objets de la Phase 5). Nouvelle méthode `d2mapentity.NPC.Knockback(source, distance)` : téléportation instantanée le long de la droite source→PNJ, sans physique/animation/collision réelles. **Première compétence multi-cible réellement testée de bout en bout** (pas seulement au niveau des données) : les tests du package `d2mapentity` peuvent construire un vrai `NPC` via `newMapEntity`, contrairement aux tests côté serveur qui ont besoin de la factory liée aux MPQ. **Limite comblée (août 2026, voir plus bas)** : aucun paquet `NPCMoved`/diffusion de position n'existait encore, donc le recul restait autoritatif côté serveur mais invisible pour les clients. Commit `cbcd5f02`.
- ✅ **Ralentissement** (3e sort d'Arcane, palier 2) : "Zone qui réduit la vitesse des entités de 50%" — sans dégâts, réutilise le mécanisme `ApplySlow`/`npcSlowedSpeedMultiplier` déjà en place pour Éclat de glace, mais sur toute une zone (`killableNPCsWithin`) au lieu d'une cible unique. Même limite documentée que le ralentissement d'Éclat de glace à l'époque : aucun paquet ne diffusait encore `SlowedUntil` aux clients (comblé plus bas, `NPCStatusEffect`). Commit `2b38a11a`.
- ✅ **Amplification** (4e sort d'Arcane, palier 2, aux côtés de Ralentissement) : "Augmente les dégâts magiques reçus par la cible" — debuff mono-cible sans dégâts propres. `d2mapentity.NPC` gagne `AmplifiedUntil`/`ApplyAmplification`/`IsAmplified`, copie conforme de `SlowedUntil`/`ApplySlow`/`IsSlowed`. `applyResolvedDamage` fait désormais passer les dégâts par `amplifiedDamage(dégâts, amplifié)` (+50%, `amplificationDamagePercent = 150`, placeholder en attendant de vrais chiffres d'équilibrage) avant de les appliquer — fonction pure extraite exprès pour être testable sans PNJ réel. Commit `891b179c`.
- ✅ **Téléportation** (5e sort d'Arcane, palier 3/niveau 12) : "Déplacement instantané vers la position visée" — déplace le lanceur lui-même, pas un PNJ. Nouveau paquet `PlayerTeleportedPacket`, volontairement distinct de `MovePlayerPacket` (qui fait cheminer une marche fluide côté client) : `handlePlayerTeleportedPacket` fixe directement `Position` via le champ promu `d2vector.Position.Set`, sans cheminement. Première compétence de ce lot testée avec un vrai test comportemental côté serveur (pas seulement un no-op) : elle ne touche que `HeroState`, aucun PNJ nécessaire. Commit `e0791e2f`.
- ✅ **Orbe glaciale** (7e sort d'Élémentalisme, palier 4/niveau 18) : "Projectile lent, explose en large AoE de froid" — même forme AoE-sur-cible que Boule de feu/Tempête statique, simplement enregistrée dans `aoeAtTargetRadiusSubtiles` avec le plus grand rayon des trois (6, contre 3/4), au prix de dégâts plus faibles (même compromis que Nova de givre face à Trait de feu). Commit `f034a9e9`.
- ✅ **Prison de glace** (6e sort d'Arcane, palier 4/niveau 18) : "Immobilise un groupe d'entités" — immobilisation totale plutôt que le ralentissement partiel de Ralentissement. `d2mapentity.NPC` gagne `ImmobilizedUntil`/`ApplyImmobilize`/`IsImmobilized` (même forme que `SlowedUntil`) ; `ChasePlayer` vérifie `IsImmobilized` en premier et force la vitesse à 0, l'immobilisation l'emportant totalement sur un ralentissement simultané plutôt que de s'y cumuler. Même limite documentée que Ralentissement à l'époque : aucun paquet ne diffusait encore `ImmobilizedUntil` aux clients (comblé plus bas, `NPCStatusEffect`). Commit `7665b051`.
- ✅ **Vortex** (7e sort d'Arcane, palier 5/niveau 24) : "Aspire toutes les entités proches vers un point" — l'inverse de Télékinésie. Nouvelle méthode `d2mapentity.NPC.Pull(cible, distance)`, symétrique de `Knockback` mais bornée pour ne jamais dépasser la cible (contrairement à `Knockback`, un no-op si le PNJ est déjà sur la cible plutôt qu'un fallback directionnel). Sort d'Arcane du plus haut palier implémenté à ce stade. Même limite documentée que Télékinésie à l'époque : aucun paquet ne diffusait encore la nouvelle position aux clients (comblé ci-dessous). Commit `10758f14`.
- ✅ **`NPCMoved` : la position de Knockback/Pull devient enfin visible côté client (août 2026)** : Télékinésie et Vortex mutaient déjà `NPC.Position` de façon autoritative côté serveur, mais rien ne le communiquait aux clients — l'entité restait visuellement figée à l'ancien endroit pour tout le monde sauf le serveur, limite documentée dès l'introduction des deux sorts. Nouveau `NPCMovedPacket` (`d2networking/d2netpacket/packet_npc_moved.go`), même forme que `NPCHitPacket` mais pour la position plutôt que les PV. `GameServer.broadcastNPCMoved(npc)` le diffuse juste après chaque `Knockback`/`Pull` ; `GameClient.handleNPCMovedPacket` écrit la nouvelle position sur la copie locale du PNJ (`NPC.Position`, un champ exporté promu depuis `mapEntity` — contrairement aux champs liés à `monstatRecord`, accessible et testable depuis `d2server` sans la factory MPQ). Test de régression : construit un `*d2mapentity.NPC` directement (seul `Position`/`ID()` sont lus par `broadcastNPCMoved`, pas besoin d'un vrai monstat) et vérifie que le paquet envoyé porte bien la position courante.
- ✅ **`NPCStatusEffect` : les effets temporisés (ralentissement, immobilisation, amplification, résistance supprimée) deviennent visibles côté client (août 2026)** : même limite que `NPCMoved` mais pour six points d'application différents — Éclat de glace, Ralentissement, Distorsion temporelle et le ralentissement d'Armure de glace sur l'attaquant (`ApplySlow`), Prison de glace (`ApplyImmobilize`), Amplification (`ApplyAmplification`) et Rupture arcane (`ApplyResistanceStrip`) mutaient tous la copie serveur du PNJ sans jamais le dire aux clients, chacun documenté comme une limite connue en son temps. Plutôt qu'un paquet par effet, un seul `NPCStatusEffectPacket` générique (`entityId`, `effect` — une des 4 constantes `NPCStatus*`, `until`) couvre les quatre, puisqu'ils partagent tous la même forme (une entité + une expiration). `GameServer.broadcastNPCStatusEffect(npc, effet, until)` est appelé juste après chacun des six sites d'application ci-dessus ; `GameClient.handleNPCStatusEffectPacket` fait un `switch` sur le nom de l'effet pour rappeler **la même méthode `NPC` que le serveur a utilisée** (`ApplySlow`/`ApplyImmobilize`/`ApplyAmplification`/`ApplyResistanceStrip`) plutôt que de dupliquer la logique côté client. Test de régression : même technique que `NPCMoved`, construit un `*d2mapentity.NPC` directement et vérifie que le paquet envoyé porte le bon effet et la bonne expiration.
- ✅ **Météore** (8e sort d'Élémentalisme, palier 5/niveau 24) : "Frappe retardée sur une zone, dégâts feu massifs" — même forme AoE-sur-cible que Boule de feu/Tempête statique/Orbe glaciale, plus haut `base_sort` de tous les sorts Devil à ce stade. La partie "retardée" n'est pas modélisée, même simplification que la "zone persistante" de Tempête statique. L'ancienne assertion de test "Boule de feu a les plus hauts dégâts de tous les sorts" a été retirée (devenue fausse) au profit d'une assertion équivalente sur Météore. Commit `7bf12f25`.
- ✅ **Distorsion temporelle** (8e sort d'Arcane, palier 6/niveau 30 — plus haut palier d'Arcane) : "Ralentit toutes les entités à l'écran pendant 5 secondes" — contrairement à la durée placeholder de Ralentissement, le design donne ici un chiffre exact (5s), utilisé tel quel. Réutilise `ApplySlow` sur tous les monstres tuables de la carte, même choix de modélisation "à l'écran" que Champ statique. Sans dégâts. Commit `49ccd754`.
- ✅ **Apocalypse** (9e sort d'Élémentalisme, palier 6/niveau 30 — l'ultime) : "Pluie d'éclairs + feu + froid sur toute la zone visible" — plus haut `base_sort` et coût en mana de tous les sorts Devil. Contrairement aux dégâts en % de la vie de Champ statique, celui-ci passe par le chemin normal `resolveAttackDamage`/`applyHit` (mise à l'échelle Energy), même choix de modélisation "à l'écran" que Champ statique/Distorsion temporelle. Commit `2059beee`.
  - **Correction** : le commit `2059beee` annonçait à tort "Élémentalisme tree complete (10/10)" — en réalité 9/10 : il manque **Maîtrise élémentaire** (palier 4, passif, "+% dégâts élémentaires par point"), qui bute sur la même limite que tout autre passif à scaling par point : `LearnSkill` refuse actuellement de réapprendre une compétence déjà connue, donc aucun mécanisme n'existe encore pour investir plusieurs points dans un même sort.
- ✅ **Bouclier de mana** (1er sort d'Ésotérisme, palier 1) : "Absorbe les dégâts avec la réserve de mana" — l'absorption (`HeroStatsState.ApplyDamageWithManaShield`) existait déjà depuis une phase antérieure et était déjà branchée dans `tryMonsterAttack` ; il manquait juste un vrai déclencheur pour le joueur. `resolveBouclierDeManaHit` bascule simplement `ManaShieldActive`. Commit `a0ca1f94`.
- ✅ **Armure de glace** (2e sort d'Ésotérisme, palier 2/niveau 6) : "Réduit les dégâts reçus et ralentit les attaquants au contact" — bascule comme Bouclier de mana (`HeroStatsState.ArmureDeGlaceActive`). `tryMonsterAttack` réduit désormais les dégâts entrants et ralentit le PNJ attaquant tant que la compétence est active, via un nouveau `npcByID` (l'attaquant n'était identifié que par son ID, pas par une référence vivante — la signature existante a été conservée plutôt que de faire passer un `*d2mapentity.NPC`, ce qui aurait cassé le seul test côté serveur capable d'exercer ce chemin sans la factory PNJ liée aux MPQ). Commit `c8935c6b`.
- ✅ **Éveil du Nexus** (3e sort d'Ésotérisme, palier 6/niveau 30 — l'ultime) : "immunité magique pendant 8 secondes, soigne le Mage" — comme Distorsion temporelle, le design donne une durée exacte (8s), utilisée telle quelle ; le montant du soin n'est pas spécifié (`eveilDuNexusHealPercent = 30`, placeholder documenté). `HeroStatsState` gagne `Heal` (inverse d'`ApplyDamage`) et `MagicImmuneUntil`/`ApplyMagicImmunity`/`IsMagicImmune` (même forme que les champs côté PNJ). `tryMonsterAttack` vérifie désormais l'immunité en premier via un `switch`, l'emportant totalement sur la réduction partielle d'Armure de glace plutôt que de s'y cumuler. Commit `1ddd109d`.
- ✅ **Correction (août 2026) — les trois sorts ci-dessus ne diffusaient jamais rien aux clients, pas même en solo** : `resolveBouclierDeManaHit`/`resolveArmureDeGlaceHit` basculaient `ManaShieldActive`/`ArmureDeGlaceActive` et `resolveEveilDuNexusHit` posait `MagicImmuneUntil` + soignait, tout ça uniquement côté serveur — aucun des trois n'envoyait le moindre paquet. Comme même le solo repasse par un client/serveur local (voir "Ce qu'on ne fait pas maintenant"), la copie cliente de `HeroStatsState` ne l'apprenait jamais, contrairement à absolument tous les autres mécanismes de cette session. Nouveau `PlayerStatusEffectPacket` (`entityId`→`playerId`, `effect`, `active`, `until` — même forme générique que `NPCStatusEffectPacket`, cette fois pour les 3 effets du joueur lui-même) diffusé par `GameServer.broadcastPlayerStatusEffect` après chacun des trois. Le soin d'Éveil du Nexus était *aussi* invisible : plutôt qu'un nouveau paquet, il réutilise `PlayerDamagedPacket` existant (qui porte juste les PV courants, pas un delta — un soin ou un dégât s'y encodent identiquement). `GameClient.handlePlayerStatusEffectPacket` applique le changement sur la copie locale du joueur.
- ✅ **Absorption d'énergie** (4e sort d'Ésotérisme, palier 3/niveau 12) : "Chaque entité tuée restaure un % de mana" — premier **vrai** passif de Devil (contrairement à Bouclier de mana/Armure de glace, jamais lancé, aucun branchement dans `resolveMeleeHit` : il consulte simplement `HeroState.Skills` à chaque mise à mort). Ne bute pas sur la limite d'investissement multi-points qui bloque Maîtrise élémentaire/Résonance magique, puisqu'un restore à plat n'a besoin que d'un état appris/non-appris, pas d'un scaling par point. `HeroStatsState` gagne `RestoreMana` (équivalent de `Heal` pour le mana). Câblé dans `applyResolvedDamage`, donc couvre tous les chemins de mise à mort (cible unique, chaîne, AoE, Apocalypse) de façon uniforme. Commit `9c7a7914`.
  - **Correction (août 2026)** : même trou que Bouclier de mana/Armure de glace/Éveil du Nexus — `restoreManaOnKill` ne diffusait rien du tout, la copie cliente du joueur n'apprenait jamais le mana restauré. Réutilise `PotionUsedPacket` existant (il porte juste le nouveau total de Mana, rien de spécifique à une potion) plutôt qu'un nouveau type de paquet pour la même forme.
- ✅ **Transcendance** (5e sort d'Ésotérisme, palier 4/niveau 18) : "Au lieu de mourir, le Mage se régénère une fois par zone (longue recharge)" — passif comme Absorption d'énergie (appris/non-appris suffit, jamais lancé). `tryTranscend` vérifie `HeroState.Skills`, se bloque via une nouvelle map de recharge `lastTranscendanceAt` (même schéma que `lastCastAt`/`lastMonsterAttackAt`), soigne à 100% de `MaxHealth` (placeholder), et annule la mort dans `tryMonsterAttack` si elle se déclenche. "Une fois par zone" n'a pas encore de sens réel (aucun événement de changement de zone n'existe) — une recharge plate de 5 minutes fait office de substitut, documenté en `ponytail`. Commit `2299c23f`.
- ✅ **Tempête de lames** (6e sort d'Ésotérisme, palier 5/niveau 24) : "Invoque des lames de mana orbitant autour du Mage" — même forme AoE-centrée-sur-le-lanceur que Nova de givre. `resolveNovaHit` a été généralisé en `resolveSelfCenteredAoeHit(source, sortID, rayon)` + une map `selfCenteredAoeRadiusSubtiles`, miroir exact de `aoeAtTargetRadiusSubtiles` pour les sorts centrés sur la cible — maintenant qu'il y a deux sorts auto-centrés, le dispatch n'empile pas non plus les if. La partie "orbitant/périodique" n'est pas modélisée, même simplification que la zone "persistante" manquante de Tempête statique. Commit `c1f6e6aa`.
- **Correction (août 2026) — la résistance magique des monstres n'a jamais manqué, elle était juste ignorée** : cette section affirmait depuis plusieurs commits que Rupture arcane était "bloqué tant qu'aucune donnée de résistance de monstre n'existe". C'était faux : `d2records.MonStatRecord` charge déjà `ResistancePhysical/Magic/Fire/Lightning/Cold/Poison` × `Normal/Nightmare/Hell` depuis monstats.txt (avec la sémantique D2 réelle : négatif = prend plus de dégâts, ≥100 = immunité) — seulement, rien dans `applyResolvedDamage` ne les lisait, donc tout monstre tuable encaissait ses dégâts sans aucune mitigation, résistance ou pas. Corrigé : `d2mapentity.NPC.MagicResistancePercent()` (lit `ResistanceMagicNormal`, `ponytail:` toujours Normal comme `HPRangeForDifficulty`) et `applyResolvedDamage` appelle désormais `d2hero.MitigateDamage(dégâts, résistance)` après l'amplification, pour **tous** les monstres tuables, pas seulement pour Rupture arcane — un vrai bug de dégâts non mitigés, corrigé indépendamment de la compétence. Contrairement à `CapResistance` côté joueur, la résistance des monstres n'est **pas** clampée à [0, 75] : cette règle "jamais négative" est spécifique à la défense du Mage (§6), pas aux monstres. Commit `<en attente>`.
- ✅ **Rupture arcane** (7e sort d'Arcane, palier 3/niveau 12, aux côtés de Téléportation) : "Projectile qui supprime les résistances d'une cible" — débloqué par la correction ci-dessus. Debuff mono-cible comme Amplification : `d2mapentity.NPC` gagne `ResistanceStrippedUntil`/`ApplyResistanceStrip`/`IsResistanceStripped` (copie conforme de `AmplifiedUntil`/`ApplyAmplification`/`IsAmplified`) ; `applyResolvedDamage` force la résistance à 0 tant que l'effet est actif, avant `MitigateDamage`. Durée placeholder de 4s (`ruptureArcaneDuration`), même valeur que celle d'Amplification faute de chiffre précisé par le design.
- ✅ **Investissement multi-points + Maîtrise élémentaire (août 2026)** : la limite citée depuis le commit `2059beee` ("`LearnSkill` refuse de réapprendre une compétence déjà connue, donc aucun mécanisme n'existe pour investir plusieurs points dans un même sort") est levée sans toucher `LearnSkill` lui-même (qui garde son comportement testé "refuse un deuxième apprentissage") : nouveau `HeroState.InvestSkillPoint(sortID)`, qui exige au contraire que le sort soit **déjà** connu, incrémente son `SkillPoints`, et consomme un point de compétence — même forme que `LearnSkill` sans dupliquer sa logique de gating (niveau/points disponibles). Maîtrise élémentaire ("Augmente tous les dégâts élémentaires (+% par point)", palier 4/niveau 18, aux côtés d'Orbe glaciale) devient le premier vrai consommateur : `MaitriseElementaireDamagePercent(points)` (5%/point, placeholder faute de chiffre du design) est appliqué dans `resolveAttackDamage` après le modificateur d'arme, filtré sur `DevilSkills[sortID].Tree == TreeElementalisme` pour ne pas déborder sur les deux autres arbres — couvre uniformément tout sort d'Élémentalisme puisque `resolveChainHit`/`resolveAoeHit`/`resolveSelfCenteredAoeHit` passent tous par `applyHit`/`resolveAttackDamage`. **Reste manquant** : le bouton UI correspondant, comme pour `LearnSkill`.
- ✅ **Vrai déclencheur réseau pour `InvestSkillPoint` (août 2026)** : nouveaux paquets `InvestSkillPointRequestPacket`/`SkillPointInvestedPacket`, même forme que `LearnSkillRequest`/`SkillLearned` — le paquet de réponse porte à la fois le nouveau total de points investis dans le sort (`InvestedPoints`) et les points de compétence restants du joueur (`SkillPoints`), puisque contrairement à `LearnSkill` la valeur qui change côté sort n'est pas juste "connu/inconnu". Côté client, `handleSkillPointInvestedPacket` ne fait rien si le sort n'est pas encore connu localement (le serveur aurait de toute façon refusé la requête). **Reste manquant** : le bouton UI, même catégorie de manque que les autres déclencheurs réseau de cette phase.
- ✅ **Correction (août 2026) — Résonance magique implémentée, ce n'était pas un mécanisme de tracking à inventer** : la section ci-dessus jugeait ce sort bloqué faute de savoir comment modéliser "chaque sort lancé augmente les dégâts du suivant (+% temporaire)". En le relisant à côté de la liste des synergies (§7 : "Résonance magique est une synergie globale : elle amplifie tous les sorts actifs des arbres I et II"), le sort se modélise en fait avec le même schéma *Until déjà utilisé partout ailleurs (`SlowedUntil`, `AmplifiedUntil`, `MagicImmuneUntil`...), simplement *consommé* à l'usage plutôt que juste testé dans le temps : `HeroStatsState.ResonanceMagiqueBonusUntil` + `ApplyResonanceMagiqueBonus`/`ConsumeResonanceMagiqueBonus` (ce dernier retourne si le bonus était actif **et** le vide dans le même appel, pour qu'il ne profite qu'à un seul sort). Armé sur **chaque** cast dispatché dans `resolveMeleeHit` (avant tout branchement spécifique à un sort, donc y compris les sorts de contrôle qui ne passent jamais par `resolveAttackDamage`), consommé dans `resolveAttackDamage` uniquement si le sort déclenché appartient à Élémentalisme ou Arcane (`ResonanceMagiqueDamagePercent(points)`, 5%/point, même convention de placeholder que Maîtrise élémentaire) — fenêtre de 3s (`resonanceMagiqueWindow`), même pratique de placeholder que les autres durées ("temporaire" sans chiffre précisé). `SkillResonanceMagique` (Arcane, palier 4/niveau 18, aux côtés de Prison de glace) bénéficie du même `InvestSkillPoint` multi-points que Maîtrise élémentaire. **Élémentalisme et Arcane sont désormais tous deux à 10/10.**
- ✅ **Correction (août 2026) — Champ statique ne profitait jamais de Résonance magique** : Champ statique calcule ses dégâts en % de la vie actuelle (`resolveChampStatiqueHit`), un chemin totalement séparé de `resolveAttackDamage` — la consommation du bonus de Résonance magique vivait uniquement dans `resolveAttackDamage`, donc Champ statique (pourtant un vrai "sort actif" d'Arcane, exactement dans le champ "tous les sorts actifs des arbres I et II" du §7) ne le consommait jamais. Corrigé en extrayant la logique dans `resonanceMagiqueBonusPercent(state, sortID)`, partagée par les deux chemins de dégâts (le seul autre existant). Test de régression : appelle directement le helper avec `SkillChampStatique` et vérifie qu'il consomme bien le bonus armé.
- ✅ **Correction (août 2026) — le 4e sort d'Ésotérisme manquant identifié : Régénération accélérée, pas bloqué du tout** : la précédente liste "4 sorts d'Ésotérisme (Familier, Double ésotérique, Golem arcane)" n'en nommait que 3 pour un compte de "4" — en recomptant contre la table du §7 (10 compétences par arbre), le 4e manquant est **Régénération accélérée** ("Augmente la vitesse de régénération du mana", palier 1, aux côtés de Bouclier de mana), qui n'a rien à voir avec le mur des invocations alliées : un mécanisme de régénération de mana existe déjà (`manaRegenPerSecond`, alimenté par l'Energy), il suffisait de brancher un bonus dessus. `RegenerationAccellereePercent(points)` (10%/point, placeholder) s'applique dans `canCastNow`, au même endroit que la lecture d'`effectiveEnergy` pour ce calcul, avant que `manaRegenPerSecond` × le temps écoulé ne détermine combien de mana regénérer. Même `InvestSkillPoint` multi-points que Maîtrise élémentaire/Résonance magique. **Ésotérisme passe de 6/10 à 7/10.**
- Reste 3 sorts d'Ésotérisme, tous les trois de vraies invocations alliées (Familier, Double ésotérique, Golem arcane) — le mur documenté plus haut (`MapEntityFactory.NewNPC` exige un vrai `MonStat2Record` + un sprite chargé, aucun des deux n'existe pour ces trois compétences).
- ✅ **Correction (août 2026) — le modificateur Feu d'une arme s'appliquait à tous les sorts, pas seulement à ceux de Feu** : `resolveAttackDamage` lisait `ItemFireDamagePercent` de l'arme équipée et l'appliquait systématiquement, quel que soit le sort réellement lancé — un bâton "+10% dégâts Feu" boostait aussi Éclat de glace (froid) et Éclair en chaîne (foudre), documenté comme limite connue depuis l'introduction du modificateur ("pas de type élémentaire par sort"). Corrigé via un nouveau `DevilSkillDef.DealsFireDamage bool` (un booléen plutôt qu'un champ Élément multi-valeurs : Feu est le seul élément pour lequel un objet Devil donne un bonus aujourd'hui — Cold/Lightning suivraient le même schéma le jour où un objet les couvre), vrai pour Trait de feu/Boule de feu/Météore/Apocalypse (celui-ci inclut le feu parmi ses trois éléments, "Pluie d'éclairs + feu + froid") d'après leur propre description dans `devil_game_design_reference.md` §7. `resolveAttackDamage` ne consulte plus le modificateur Feu que si `DealsFireDamage` est vrai.
- **⚠️ Blocage précisé (août 2026)** : `MapEntityFactory.NewNPC` (`d2core/d2map/d2mapentity/factory.go`) — le seul moyen d'instancier un `NPC` — exige (1) un `*d2records.MonStatRecord` **et** son `MonStat2Record` associé déjà chargé dans `asset.Records.Monster.Stats2[monstat.ExtraDataKey]` (indexé par une clé issue de monstats.txt réel — un `MonStatRecord` synthétique pour "Familier" ne résoudrait pas, et `result.monstatEx.EquipmentOptions` planterait sur un pointeur nil juste après), et (2) un vrai sprite chargé via `asset.LoadComposite` (fichier DC6/DCC sur disque) — aucun sprite Familier/Golem n'existe. Ajouter un simple champ `IsAlly bool` sur `NPC` (façon `IsKillable`) resterait insuffisant tant que ces deux dépendances ne sont pas levées : ce n'est donc pas un problème de modèle de données côté `NPC`, mais l'absence de données de monstre + sprite pour ces trois compétences spécifiquement — même mur que la Phase 6, pas un nouveau chantier séparé.
- ✅ **Respec (moitié compétences)** : `HeroState.RespecSkills()` — vide les compétences apprises, rembourse leurs points, remet `LeftSkill`/`RightSkill` à 0 (sinon ils pointeraient sur une compétence qui n'existe plus). Couvre la moitié "compétences" du **Respec partiel** (§10). La moitié "attributs" n'est pas modélisée : `Strength`/`Energy`/`Dexterity`/`Vitality` n'ont aucun suivi des points investis (seul le total courant existe, indiscernable de la valeur de base) — un vrai manque, documenté plutôt que comblé par un mécanisme de suivi inventé sans qu'on sache s'il correspond à l'UI existante. Commit `ee07f636`.
- ✅ **Glyphe d'oubli (moitié compétences)** : `HeroState.RespecSingleSkill(sortID)` — oublie une seule compétence et rembourse son point, sans toucher aux autres compétences apprises ni à l'emplacement équipé de l'autre. Même limite que `RespecSkills` pour la moitié "attribut". Commit `f145308a`.
- ✅ **Correction (août 2026) — la moitié "attributs" comblée : allocation de points d'attributs + les deux respecs complétés** : le manque ci-dessus tenait à l'absence totale d'un mécanisme de dépense de points d'attributs, pas juste d'un suivi pour le respec — `HeroStatsState.StatsPoints` (§6 : "5 points d'attributs à répartir librement" par niveau) s'accumulait déjà depuis `GrantExperience` mais rien ne pouvait le dépenser. Nouveau `HeroStatsState.SpendAttributePoint(attr)` (`Attribute` : Strength/Energy/Dexterity/Vitality) incrémente l'attribut choisi **et** un compteur `*Spent` dédié (`StrengthSpent`/`EnergySpent`/`DexteritySpent`/`VitalitySpent`) — nécessaire puisque l'attribut courant seul ne distingue pas la valeur de base de ce qui a été dépensé. Ça débloque à la fois : `HeroStatsState.RespecAllAttributePoints()` (rembourse tout, branché dans `RespecSkills` — **Respec partiel** couvre maintenant vraiment "tous les points de compétences et d'attributs", §10) et `HeroStatsState.RefundAttributePoint(attr)`/`HeroState.RespecSingleAttributePoint(attr)` (rembourse un seul point sur un seul attribut — la moitié "1 point d'attribut" de **Glyphe d'oubli**, symétrique de `RespecSingleSkill` sans réutiliser sa signature puisqu'un même objet ne rembourse qu'une compétence OU un attribut, pas les deux). **Reste manquant** : le panneau d'allocation de points côté client.
- ✅ **Vrais déclencheurs réseau pour `SpendAttributePoint`/`RespecSingleAttributePoint` (août 2026)** : deux nouvelles paires de paquets, même forme que `LearnSkillRequest`/`SkillLearned` — `SpendAttributePointRequest`/`AttributePointSpent` et `RespecSingleAttributePointRequest`/`SingleAttributePointRespeced`. Les deux réponses portent la nouvelle valeur de l'attribut concerné et le nouveau total de `StatsPoints` du joueur ; `d2hero.Attribute` traverse le réseau comme un simple `int`, reconstruit et validé côté serveur par `SpendAttributePoint`/`RespecSingleAttributePoint` elles-mêmes (une valeur hors des quatre attributs connus échoue silencieusement, comme un ID de compétence inconnu). **Reste manquant** : le panneau d'allocation de points côté client, seul morceau non couvert par cette phase.
- ✅ **Correction (août 2026) — les paquets de dépense/remboursement d'attribut ne portaient pas leurs propres effets de bord** : trouvée en creusant le même trou que le mana (juste au-dessus) — `SpendAttributePoint`/`RefundAttributePoint` font grandir/rétrécir `MaxHealth`/`Health`/`MaxMana`/`Mana` en plus de l'attribut lui-même (Vitality/Energy), mais `AttributePointSpentPacket`/`SingleAttributePointRespecedPacket` ne portaient que l'attribut touché et `StatsPoints` — la copie cliente du joueur ne grandissait/rétrécissait donc jamais ses réserves de vie/mana, même si le serveur le faisait bien. Même trou, en pire, pour `SkillsRespecedPacket` (Respec partiel) : `RespecSkills()` rembourse **les quatre** attributs à la fois via `RespecAllAttributePoints()`, mais le paquet ne portait que `SkillPoints`. Les trois paquets portent désormais `MaxHealth`/`Health`/`MaxMana`/`Mana` (et `SkillsRespecedPacket` porte en plus `StatsPoints`/`Strength`/`Energy`/`Dexterity`/`Vitality`, puisque n'importe lequel des quatre peut avoir changé). Tests de régression pour les trois paquets, vérifiant que le paquet diffusé porte bien les valeurs dérivées après une dépense/un remboursement de Vitality.
- ✅ **Correction (août 2026) — dépenser un point de Vitality/Energy ne faisait rien** : signalé comme un vrai manque à deux reprises plus haut ("précision honnête", Phase 5) — `MaxHealth`/`MaxMana` n'étaient calculés qu'une seule fois, dans `CreateHeroStatsState` (`classStats.InitVit * classStats.LifePerVit`), et rien ne les recalculait ensuite. Tant qu'aucun mécanisme de dépense n'existait, cette staleness était sans conséquence ; `SpendAttributePoint` la rend réelle : un joueur qui investit dans Vitality ("Détermine les points de vie", §5) ne gagnait aucun point de vie. Corrigé en conservant `LifePerVit`/`ManaPerEne` (colonnes charstats.txt) sur `HeroStatsState` elle-même au moment de la création, pour que `SpendAttributePoint`/`RefundAttributePoint`/`RespecAllAttributePoints` puissent ajuster `MaxHealth`/`Health` et `MaxMana`/`Mana` en cohérence à chaque dépense ou remboursement (`Health`/`Mana` courants sont clampés au nouveau maximum plutôt que naïvement décrémentés, pour ne jamais faire "perdre" de vie/mana à un joueur qui en avait moins que le maximum au moment d'un respec). **Ne couvre pas** l'autre moitié du même manque documenté en Phase 5 : un bonus `HealthBonus`/`ManaBonus` d'objet (Robe du Novice) reste appliqué une seule fois à l'équipement, toujours pas recalculé si l'objet change — un chantier distinct (`effectiveMaxHealth`/`effectiveMaxMana` façon `effectiveEnergy`, pas fait ici).
- ✅ **Vrais déclencheurs réseau pour `RespecSkills`/`RespecSingleSkill` (août 2026)** : nouveaux paquets `RespecSkillsRequest`/`SkillsRespeced` et `RespecSingleSkillRequest`/`SingleSkillRespeced`, même forme que `UsePotionRequest`/`PotionUsed` et `LearnSkillRequest`/`SkillLearned`. Côté client, `Player.LeftSkill`/`RightSkill` sont des pointeurs `*d2hero.HeroSkill` (contrairement aux ID entiers de `HeroState`) — comparés via `SkillRecord.ID` pour savoir s'ils référençaient la compétence oubliée. **Reste manquant** : un vrai bouton respec/objet Glyphe d'oubli côté client — même catégorie de manque que les autres déclencheurs UI. Commit `b032dbbb`.
- **Reste à faire** : le déclencheur réel de Glyphe d'oubli (objet rare, drop ou craft) et **Respec complet** (4 essences de boss via le Cube de Nexus) — ce dernier pourrait réutiliser le moteur de craft (`devil_crafting.go`) une fois que le modèle de recette supporte plusieurs objets en entrée.
- ✅ **Correction (août 2026) — Respec partiel n'appliquait jamais sa propre limite "1 fois par difficulté"** : le tableau du §10 précise l'accès à Respec partiel comme "1 fois par difficulté (quête Den of Nexus)", mais `RespecSkills()` n'avait aucune limite d'usage — utilisable un nombre illimité de fois. Nouveau `HeroState.RespecPartielUsedAt map[d2enum.DifficultyType]bool` (même forme que `DiscoveredRecipes` juste au-dessus) ; `RespecSkills()` retourne désormais une erreur si déjà utilisé à `h.Difficulty` (changement de signature : `(pointsRefunded int)` devient `(pointsRefunded int, err error)`, seul appelant en prod mis à jour). **Ne couvre pas** la moitié "quête Den of Nexus" de la même règle — aucun système de quête n'existe pour la gate derrière un vrai déblocage narratif, documenté plutôt qu'imité par un flag inventé. Vérifié dans le round-trip de sauvegarde existant.
- ✅ **Premier fichier de test pour `d2networking/d2client` (août 2026)** : ce paquet n'avait jamais eu le moindre test, alors qu'il compte maintenant une quinzaine de gestionnaires de paquets, plusieurs ajoutés dans cette même session sans jamais être vérifiés au-delà de "ça compile". `game_client_test.go` couvre désormais **14 des ~16 gestionnaires** : les 7 ajoutés cette session (effets de statut du joueur, dépense/remboursement d'attribut, respec complet, craft, investissement de point de compétence) plus 7 antérieurs qui, en y regardant, ne touchaient eux aussi que `GameClient.Players` (`SkillLearned`, `SingleSkillRespeced`, `PotionUsed`, `PlayerDamaged`, `GoldAwarded`, `ExperienceAwarded`, `PlayerTeleported`). **Ne couvre pas** `handleNPCMovedPacket`/`handleNPCStatusEffectPacket`/`handleNPCHitPacket`/`handleSpawnItemPacket` (ont tous besoin de `MapEngine.Entities()`/`MapEngine.NewItem`, qui n'existent que contre un vrai gestionnaire d'assets chargé depuis les MPQ — même mur documenté partout ailleurs côté `d2server`) — une couverture large mais volontairement pas totale, plutôt qu'une prétention à un paquet "testé" en entier.

**Pourquoi Élémentalisme en premier :** c'est l'arbre qui valide le plus directement la formule de dégâts déjà posée en Phase 1, sans dépendre de mécaniques qui n'existent pas encore (contrôle de groupe, invocations alliées).

## Phase 3 — Scripting moderne ✅
*Fait — commit `5c544c9c`.*

- ✅ `d2script` utilisait `otto` (interpréteur JS pur Go sans activité depuis des années). Remplacé par `wazero` (runtime WASM pur Go, zéro cgo, activement maintenu) : `ScriptEngine` charge et exécute des modules `.wasm` compilés à l'avance plutôt que d'interpréter du texte JS.
- ✅ `Eval`/`AllowEval`/`DisallowEval` supprimés entièrement — pas d'équivalent WASM ("évaluer une chaîne de code arbitraire" n'existe pas dans ce modèle), et c'est une vraie amélioration de sécurité : un module WASM ne peut appeler que les fonctions hôte qui lui sont explicitement données, jamais un accès arbitraire au process comme avec `Eval()`.
- ✅ Test de bout en bout réel (pas juste la plomberie à vide) : le toolchain Go standard compile directement en WASM (`GOOS=wasip1 GOARCH=wasm go build`, sans tinygo/clang) — un vrai module invité compilé (`d2script/testdata/hello.wasm`) importe et appelle une fonction hôte, le test vérifie que la valeur passée arrive bien côté Go.
- La compétence `getMapEngines` (exposait tout le slice `[]*MapEngine` via le passage de valeur arbitraire d'otto) ne se traduit pas en WASM — un module invité ne peut pas garder une référence Go vivante. Remplacée par un placeholder minimal (`mapEngineCount`, retourne un compte) qui prouve juste que le mécanisme fonctionne. Rien n'exploite ce point d'accroche aujourd'hui de toute façon.
- Reste à faire : c'est le mécanisme qui portera la **narration** (Phase 4) — déclencher les dialogues de Sage Wyn, la révélation finale, les quêtes d'urgence des Events temporaires (§9) — mais l'API de script réelle (quelles fonctions hôte exposer, quel format de module invité pour une "quête") n'est pas encore conçue, seulement le mécanisme de base.

## Phase 4 — Contenu narratif & Région I
*Après qu'un sort fonctionne (Phase 1) et que le scripting moderne soit en place (Phase 3).*

Construit la première tranche de contenu jouable racontée, sur la Région I (Terres dévastées — "village natal de Devil, aujourd'hui méconnaissable", `devil_lore.md` §6).

- ✅ **IA de monstre minimale** : premier tick périodique côté serveur (`runMonsterAILoop`, 200ms — le serveur était jusqu'ici purement réactif aux paquets, n'avançait jamais rien de lui-même). Un monstre tuable détecte le joueur connecté le plus proche, le poursuit en ligne droite (`NPC.ChasePlayer`, pas de vrai pathfinding autour des obstacles), et l'attaque à portée avec son propre cooldown. Nouveau paquet `PlayerDamaged` pour synchroniser les PV du joueur au client. Commit `9e8fac4c`.
- ✅ **Registre d'archétypes de monstre** (`d2core/d2hero/devil_monster_archetypes.go`) : `MonsterArchetypeDef` + registre `MonsterArchetypes`, même forme struct+registre+lookup-avec-repli que `DevilSkillDef`/`DevilItemDef`. Nouvelle méthode `d2mapentity.NPC.MonsterKey()` (expose `MonStatRecord.Key`, "Id" dans monstats.txt). Contrairement à un simple modèle de données spéculatif, c'est réellement branché : `advanceMonsterAI`/`tryMonsterAttack` consultent désormais le registre via la clé du PNJ avant de retomber sur les constantes à plat existantes. Le registre est vide pour l'instant (même mur que les compétences d'invocation et les assets visuels — aucune donnée de monstre Devil n'existe), donc le comportement de chaque monstre est inchangé aujourd'hui, mais le jour où de vraies données existent, le réglage par type de monstre fonctionne immédiatement, sans nouveau changement côté serveur. Commit `85f75199`.
- ✅ **`MonStatRecord.HPRangeForDifficulty` (août 2026)** : `devil_game_design_reference.md` §1 — "Chaque région est rejouable en trois niveaux de difficulté [...] Les entités y sont plus puissantes." En creusant, `MapEntityFactory.NewNPC` utilise toujours `MinHPNormal`/`MaxHPNormal` pour tirer les PV d'un monstre, quelle que soit la vraie difficulté de la partie — alors que `MonStatRecord` a déjà les vraies colonnes `MinHPNightmare`/`MaxHPNightmare`/`MinHPHell`/`MaxHPHell` chargées depuis monstats.txt, simplement jamais lues. Ajout d'un lookup pur `HPRangeForDifficulty(difficulté)`. **Volontairement pas branché dans `NewNPC`** : ça demanderait de faire transiter une valeur de difficulté par 3 points d'appel côté client (`game_client.go`, `d2gamescreen/game.go`, `d2mapstamp/stamp.go`) que cet environnement sans affichage ne peut pas vérifier visuellement sans faire tourner le vrai jeu — même limite de risque que les autres changements restés côté serveur cette session. Premier fichier de test pour `d2core/d2records` (paquet jusque-là sans aucun test). Commit `2f728aa6`.
- **Gardien des Cendres** comme boss de fin de région : un monstre unique (pas une simple instance de monstat.txt générique), avec ses propres PV/résistances/comportement — construit par-dessus l'IA minimale ci-dessus.
- **Dialogues de Sage Wyn** (`devil_lore.md` §7) au démarrage et avant la salle du boss — premiers contenus scriptés via le pipeline WASM de la Phase 3.
- **Narration à la mort du boss** — valide qu'un événement de gameplay (mort d'un monstre) peut déclencher un texte narratif, brique nécessaire pour la révélation finale du jeu plus tard.
- **Une quête complète de bout en bout** sur cette région, pour valider tout le pipeline narratif avant d'en écrire pour les 4 autres régions.
- **Génération procédurale améliorée** (`devil_game_design_reference.md` §4.1) : mini-map toujours visible et mise à jour en temps réel (actuellement la mini-map D2 est optionnelle/incomplète), tiles sans labyrinthes sans issue, icônes de points d'intérêt (boss, waypoints, coffres rares) dès leur découverte. Améliorations du moteur `d2mapgen`/`d2maprenderer`, pas juste du contenu.

## Phase 5 — Objets, loot & progression 🚧
*Démarrée.*

- ✅ **Modificateur d'arme** : voir Phase 1 (Bâton de l'Apprenti, `+10% dégâts Feu`). Commit `3578d906`.
- ✅ **Or à la mort d'un monstre** : `d2hero.RollGoldDrop` (5-20, plage à plat faute de données de Treasure Class par niveau de monstre), nouveau paquet `GoldAwarded`. Contourne le mur des objets qui tombent au sol (voir ci-dessous) : l'or n'a pas besoin de sprite pour exister, donc c'est un mécanisme réel de bout en bout plutôt qu'une plomberie qui ne produirait rien de visible. Commit `175070f8`.
- **⚠️ Limite structurelle des objets au sol** : faire apparaître un objet ramassable (`d2mapentity.Item`) exige un vrai sprite — `MapEntityFactory.NewItem` charge l'animation depuis `item.CommonRecord().FlippyFile`, un fichier DC6 réel. Aucun sprite d'objet Devil n'existe (même mur que l'identité visuelle du Mage, Phase 6). Tant que ça reste vrai, tout objet Devil ramassable au sol échouera à s'afficher (`NewItem` retourne une erreur, gérée sans crash côté client, mais rien n'apparaît). L'or et l'XP (Phase 2) — mécanismes sans dépendance visuelle — restent le terrain praticable de cette phase en attendant ; la réputation ou d'autres systèmes similaires suivraient le même chemin.
- **Objets magie-only** : bâtons, orbes, robes, amulettes, anneaux, grimoires — pas d'armure physique lourde, pas d'arme de corps-à-corps non magique (`devil_game_design_reference.md` §8). `items.txt` et le système d'équipement (`d2inventory.CharacterEquipment`) sont à retravailler avec ce nouveau set d'emplacements/types, pas juste réutilisés depuis D2.
  - ✅ **Emplacement amulette + Pendentif Arcane (août 2026)** : `CharacterEquipment` gagne un champ `Amulet` (`*InventoryItemMisc`, type déjà existant réutilisé plutôt qu'un nouveau) — le jeu de base n'avait jamais modélisé d'emplacement de cou. `ItemPendentifArcane` (+3 Energy) est équipé à la création de personnage aux côtés du bâton ; `effectiveEnergy` additionne désormais les bonus de `RightHand` **et** `Amulet`. Commit `52f4608a`.
  - ✅ **Emplacement anneau + Anneau du Début (août 2026)** : `CharacterEquipment` gagne `Ring` (`*InventoryItemMisc`, un seul emplacement — le design de Devil n'en liste qu'un, pas deux comme D2). `DevilItemDef` gagne `AllAttributesBonus` pour les objets qui boostent tous les attributs à la fois ("+2 à tous les attributs") plutôt qu'un seul. `dexterityOf`/`effectiveEnergy` passent par un nouveau `equippedItemCodes` partagé (RightHand/Amulet/Ring) et une nouvelle `effectiveDexterity` — **précision honnête** : seuls Energy et Dexterity ont un vrai chemin de lecture "effectif" aujourd'hui ; Strength et Vitality sont fixés une fois à la création et jamais recalculés, donc un bonus d'objet sur l'un des deux n'a encore aucun effet en jeu. Commit `59ec9b72`.
  - **Correction** : la phrase précédente affirmait à tort que les 5 objets de départ du Mage étaient "désormais tous équipables" — faux à ce stade, la Robe du Novice n'était pas encore équipée (voir juste en dessous) et la Ceinture ne l'a toujours pas été.
  - ✅ **Robe du Novice (août 2026)** : `DevilItemDef` gagne `HealthBonus`/`ManaBonus` ("+15 Vie, +5 Mana", §5) — contrairement à `EnergyBonus` (relu à chaque attaque via `effectiveEnergy`), appliqués une seule fois à l'équipement, au moment de la création du personnage, cohérent avec le fait que `MaxHealth`/`MaxMana` eux-mêmes ne sont calculés qu'une fois à la création dans cette base de code, jamais recalculés en direct. Équipée dans l'emplacement `Torso` déjà existant. Commit `f2e7b76a`.
  - ✅ **Ceinture de Cuir Runique + système de ceinture à potions (août 2026)** : `d2core/d2hero/devil_belt.go` — `HeroState.Belt` (codes d'objets par emplacement, `""` = vide), `InitBelt(capacité)`, `AddPotionToBelt`, `UsePotion(index)`. Pas de champ `CharacterEquipment.Belt` : la ceinture n'est pas un objet à bonus de stats comme le bâton/pendentif/anneau/robe, elle donne juste une *capacité* d'emplacements ("4 emplacements potions", §5 ; jusqu'à 16 une fois améliorée, `devil_game_design_reference.md` §8). `ItemPotionDeMana` ajouté comme premier consommable. **Les 5 objets de départ du Mage sont désormais tous modélisés** : Bâton de l'Apprenti, Pendentif Arcane, Anneau du Début, Robe du Novice, Ceinture de Cuir Runique. Commit `7ee0044c`.
  - ✅ **Vrai déclencheur réseau pour `UsePotion` (août 2026)** : nouveaux paquets `UsePotionRequestPacket` (client→serveur) et `PotionUsedPacket` (serveur→clients), même forme que `CastPacket`/`GoldAwardedPacket`. `GameServer.resolveUsePotion` relie enfin `HeroState.UsePotion` à une vraie requête réseau — même séquence "mécanique d'abord, déclencheur ensuite" que `LearnSkill`/`RespecSkills` en leur temps. **Reste manquant** : un vrai bouton/raccourci côté client qui envoie la requête — hors de portée ici, comme `LearnSkill` qui n'a toujours pas d'UI non plus. Commit `c7c71f62`.
  - ✅ **Correction (août 2026) — 2 des 4 points d'Anneau du Début n'avaient jamais aucun effet, pas même à la création** : la "précision honnête" ci-dessus disait juste que Strength/Vitality n'avaient pas de lecture "effective" en direct — en creusant plus loin, le problème était en réalité pire : `CreateHeroStatsState` tourne **avant** que l'équipement soit assigné dans `select_hero_class.go`, donc la composante Vitality de l'anneau n'était même pas prise en compte une seule fois à la création, contrairement à `EnergyBonus`/`HealthBonus` qui ont chacun leur propre chemin (lecture en direct ou bake-in à la création). Corrigé en appliquant `ItemAllAttributesBonus(ItemAnneauDuDebut)` à `Stats.Strength`/`Stats.Vitality` (et à `MaxHealth`/`Health` via `LifePerVit`) juste après l'équipement, exactement comme le bonus de la Robe du Novice juste au-dessus. Strength reste sans aucun effet mécanique pour le Mage (§5, supprimée), mais la donnée de l'objet est maintenant honnête plutôt que silencieusement ignorée à moitié.
- **Treasure Classes** : le concept (probabilités par TC, NoDrop, picks) est directement réutilisable depuis D2 — c'est un mécanisme, pas du contenu Blizzard.
- ✅ **Moteur de craft du Cube de Nexus** (`d2core/d2hero/devil_crafting.go`) : `DevilCraftingRecipe` + registre `DevilCraftingRecipes` (même forme struct+registre+lookup que les modèles de données Devil existants), et `HeroState.Craft(recipeID)` qui valide l'objet équipé + l'or, puis les consomme pour produire la sortie. Une vraie recette (`RecipeUpgradeBatonApprenti` : Bâton de l'Apprenti + 50 or → Bâton de l'Initié, un nouvel objet aux meilleures stats) prouve le moteur de bout en bout — `ItemFireDamagePercent` (déjà utilisé par `resolveAttackDamage`) reconnaît automatiquement le nouvel objet, aucun câblage serveur supplémentaire nécessaire. Volontairement limité à l'emplacement d'arme équipée, pas un vrai inventaire/stash (inexistant, voir plus bas). Commit `4a024dc1`.
- ✅ **Vrai déclencheur réseau pour `Craft` (août 2026)** : `Craft` avait sa mécanique depuis un moment mais aucun paquet ne l'exposait, contrairement à `LearnSkill`/`UsePotion`/`SpendAttributePoint` — même séquence "mécanique d'abord, déclencheur ensuite". Nouveaux `CraftRequestPacket`/`ItemCraftedPacket` : la réponse porte le nouveau code + nom de l'objet en `RightHand` (relu directement sur l'équipement après un `Craft` réussi, plutôt que de re-consulter la recette) et l'or restant. **Reste manquant** : le bouton UI du Cube de Nexus, même catégorie de manque que les autres déclencheurs de cette phase.
- ✅ **Correction (août 2026) — la moitié "suivi" du Codex progressif existe désormais, indépendamment de son UI** : la ligne ci-dessus classait tout le Codex ("chaque recette découverte ... est enregistrée et consultable à tout moment", §8) comme une pure fonctionnalité UI à construire — en réalité, *savoir* quelles recettes ont été découvertes est un vrai besoin côté modèle de données (et de sauvegarde), séparé de l'écran qui l'afficherait, même schéma que la ceinture de potions ou l'investissement de points de compétence avant leurs déclencheurs. Nouveau `HeroState.DiscoveredRecipes map[string]bool` + `DiscoverRecipe(recipeID)`/`HasDiscoveredRecipe(recipeID)` ; `Craft` marque désormais la recette comme découverte à chaque succès — couvre la moitié "par utilisation" du design, pas "par quête" (aucun système de quête n'existe). Vérifié dans le round-trip de sauvegarde existant. **Reste manquant** : l'écran du Codex lui-même, et la quête obligatoire de Région II qui introduit le Cube (§8) — ni l'un ni l'autre ne sont modélisés ici.
- **Inventaire/stash réels** : le Cube de Nexus ne peut aujourd'hui transformer que l'objet équipé (`Equipment.RightHand`), faute d'un vrai modèle de grille d'inventaire pour Devil — à construire avant que des recettes multi-ingrédients ou une vraie gestion de stock aient un sens.
- **Respec** : voir Phase 2.
- ✅ **Moteur de scheduling pour 2 des 4 Events temporaires (août 2026)** : `d2networking/d2server/game_server_events.go` — `ActivateEvent(nom, durée)`/`IsEventActive(nom)` génériques, même forme que les maps `lastXAt` déjà en place (réactiver un event réinitialise son expiration au lieu de cumuler les durées). Couvre **Tempête de loot** et **Nuit de l'Apocalypse** (§9), les deux seuls events à durée fixe. **Boss corrompu** (spawn rare) et **Quête d'urgence** (déclenché par la progression) ne sont pas des buffs temporisés — ce sont des effets ponctuels liés à un système de spawn/quête qui n'existe pas encore, volontairement non modélisés ici. Les magnitudes d'effet (`tempeteDeLootNoDropDivisor`, `nuitDeLApocalypseMagicFindPercent`) sont déclarées mais pas encore consommées : Devil n'a pas encore son propre moteur de drop où les brancher (voir les manques Treasure Classes/inventaire ci-dessus). Commit `24680b8b`.

## Phase 6 — Rendu, confort visuel & identité artistique
*Statut : filtrage des tuiles fait. De vrais assets existent enfin (Mage/Mage Divin/Warrior), mais pas encore au format exploitable par le moteur.*

- ✅ Filtrage linéaire sur les tuiles de sol/mur/ombre (`d2maprenderer`) — commit local `33f27a2`.
- ✅ **Prompts de génération d'assets** (`docs/visual_asset_prompts.md`) : prompts prêts à l'emploi (style Final Fantasy) pour un générateur d'images externe. Aucun outil de génération d'image n'est disponible dans cet environnement de code : ces prompts sont exécutés par un humain.
- ✅ **Premiers vrais assets reçus** (août 2026) : turnarounds 9 angles + cycles de marche pour Mage, Mage Divin (forme évoluée) et Warrior (nouvelle classe, voir la note multi-classes en tête de document). `devil_mage_character_design.md` a été intégralement réécrit pour coller à ces assets (l'ancienne palette cramoisi/or/bâton ne correspond pas à ce qui a été livré) ; `devil_warrior_character_design.md` est nouveau.
- **Toujours bloquant côté moteur** : ces rendus sont vus à hauteur d'œil (caméra écran de sélection), pas depuis la caméra isométrique du moteur. Directement exploitables pour portraits/UI/artwork de menu ; nécessitent un nouveau rendu (caméra isométrique) ou un repaint + découpage en frames + encodage `.DC6` pour devenir de vrais sprites de gameplay animés. C'est un chantier de production d'assets, pas un problème d'ingénierie — voir `devil_mage_character_design.md` §5 et `devil_warrior_character_design.md` §3.
- **Nouveau depuis le pivot multi-classes** : `devil_warrior_character_design.md` §4 liste ce qui manque pour rendre le Warrior jouable côté code (attributs, arbre de compétences, itémisation, formule de dégâts) — rien de tout cela n'existe encore, volontairement non inventé sans le design du studio.
- Pipeline d'upscaling de sprites (xBRZ/HQx offline, métadonnées de taille cohérentes) — pertinent une fois qu'il y a de vrais sprites Devil à upscaler, pas seulement les sprites D2 d'origine.
- Post-traitement via les shaders Kage natifs d'ebiten (bloom, vignette) — bon candidat pour rendre les auras de compétences (voir plus haut) plutôt qu'un simple effet cosmétique.
- Résolution interne fixée à 800×600 — support HiDPI à traiter avec le reste.

## Phase 7 — Outillage de contenu 🚧
*Démarrée.*

- ✅ **Format de mod documenté** (`docs/modding.md`) : contrat hôte/invité, comment compiler un module Go avec le toolchain standard, la seule fonction hôte qui existe aujourd'hui, limites explicites (pas d'API quête/dialogue, types numériques/mémoire seulement, pas de sauvegarde côté script). Commit `4af83004`.
- **Hors de portée ici** : finir la communication `abysswrapper` ↔ moteur, et étendre les éditeurs HellSpawner — les deux exigent le dépôt `hellspawner`/`AbyssEngine` séparé et une interface graphique (giu/ImGui) qui a besoin d'un display, indisponibles dans cet environnement — même nature de mur que la production d'assets visuels.

## Phase 8 — Packaging & diffusion 🚧
*Démarrée.*

- ✅ **Build via `goreleaser`** (`.goreleaser.yml`, schéma v2) : vérifié de bout en bout — `goreleaser build --snapshot` produit un binaire réel, exécuté pour confirmer qu'il tourne. Scope honnête : macOS uniquement (amd64+arm64), vérifié. La compilation croisée CGO vers Linux échoue proprement depuis macOS sans toolchain C croisée (testé) — Windows aurait le même problème. La vraie solution (documentée dans le fichier) : un job CI par OS natif, pas de compilation croisée locale.
- ✅ **Rappel légal au démarrage** : `d2app.Create` affiche maintenant un message rappelant que le jeu nécessite les fichiers Diablo II + LoD achetés légalement par le joueur — vérifié en exécutant le binaire buildé par goreleaser. Commit `cc6d8289`.

## Ce qu'on ne fait pas maintenant

- Refonte 3D/PBR — incompatible avec des sprites 2D pré-rendus.
- Migrer vers AbyssEngine comme base — trop tôt, aucune logique de jeu à ce stade.
- Portage natif WebGPU/Vulkan — gain marginal pour le risque.
- Multijoueur en ligne — décision de scope confirmée par le game design (§1 : "action-RPG solo"). L'architecture interne d'OpenDiablo2 fait transiter même le solo par un client/serveur local ; on la laisse telle quelle.
- Les 5 régions avant d'avoir la Région I qui tient debout.
- Réutiliser tel quel le contenu D2 (skills.txt, monstats.txt, items.txt) — le design est trop différent (classe unique, magie-only, attributs différents) pour que ça vaille le coup ; seul le *pipeline* de chargement de ces formats est réutilisé.

## Risques à surveiller

- **Production d'assets visuels** : aucun sprite Devil n'existe encore, et les sprites D2 d'origine ne correspondent pas au design (voir Phase 6). C'est probablement le vrai goulot d'étranglement du projet, pas le code moteur.
- Solo ou petite équipe : la Phase 1 corrigée + Phase 2 (30 compétences) représentent plusieurs mois, pas des semaines.
- Le combat déjà commité (`0c9f823`, `1ebdbd3`) utilise une formule physique à corriger — ne pas construire d'autres systèmes par-dessus (IA, objets) avant d'avoir corrigé la formule de base, sous peine de devoir tout reprendre.
- wazero change la façon d'écrire des quêtes/dialogues ; prévoir un exemple de bout en bout (Sage Wyn) avant de scripter le reste du contenu narratif.

## Prochaine étape

Brancher *Trait de feu* comme premier sort réel (données de compétence + `base_sort` propre), pour valider la formule Energy sur un cas concret avant d'ouvrir davantage de compétences ou de contenu.
